# TA採点プロジェクト

創生科学基礎演習I（法政大・2026秋）の課題採点を半自動化する。
提出されたJupyter Notebookを読み、点数とコメントを大学システム用のcsvに書き込む。

## 進め方

回ごとの採点は `/grade <回>` で実行する（例: `/grade 01p`）。
このファイルの判定基準は毎回必ず適用する。基準が更新されたら
「裁定履歴」に日付付きで追記する。

## ディレクトリ

```
rounds/<回>/
  *.zip                  # 大学システムからDLした提出物一式（answer.csv同梱）
  *_ans.ipynb            # 模範解答（任意）
  work/                  # zip展開先（gitignore）
  extracted.txt          # 抽出結果（gitignore）
  grades.json            # 採点結果。裁定反映時はここを直して再生成
  採点済_<問題名>.csv     # 提出用
  要判断リスト.txt        # 迷ったもの。裁定後は削除
```

## スクリプト

| コマンド | 役割 |
|---|---|
| `python3 scripts/extract.py rounds/<回>` | zip展開＋コード/出力を `extracted.txt` へ |
| `python3 scripts/extract.py rounds/<回> --range 1 10` | 人数を絞って抽出（バッチ採点用） |
| `python3 scripts/build_csv.py rounds/<回>` | `grades.json` → 採点済csv（UTF-8 BOM） |

**提出ipynbは絶対に再実行しない。** `input()` を含むため実行できず、
保存済みの出力セルが唯一の判定材料。

## grades.json の形式

```json
{
  "26X5048": {"q": {
    "1": [2, ""],
    "2": [1, "円周率が3.14"],
    "3": [0, "初期値0から加算せず"]
  }}
}
```

`[点数, コメント]`。コメントは減点分のみ、10文字程度。
csvのN列には `2. 円周率が3.14` のように問題番号を付けて改行連結される。
満点の場合は `全問正解` が自動で入る。

---

# 判定基準

version: v2（最終更新 2026-09-18）

## 配点

1問 2点。問題数 × 2 が満点（5問なら10点）。

## 0点

- 実行結果がない（セル未実行、`print` なしで出力ゼロ）
- エラーで停止している（Traceback / 例外出力）
- 解答セル自体が存在しない
- 問題の要件を満たしていない（求められた処理・出力をしていない）
- あからさまに高度なモジュールを使用している（基礎を学ぶ場のため）
  - 例: `math`, `numpy` 等で本来手計算・基本構文で書くべき処理を代替
  - 標準の言語機能（`f-string`, `try/except` 等）は対象外

## 1点（-1）

- 軽微なミス 1つ
  - 出力形式のズレ
  - 指示された定数・文言との相違
  - 出力すべき値の一部欠け
- 軽微なミスが 2つ以上 → 0点

## 2点（満点）

上記に該当しないもの。

## 減点しない（許容）

- 変数名の言語・命名（日本語、ローマ字、typo でも動作すれば可）
- 代入した値の中身の違い（名前・年齢・半径 など）
- 「！」の有無、送り仮名・表記の差（「来ました」/「きました」）
- `if/if` と `if/else`、`elif` の書き分け
- `input()` で値を受け取る実装（変数を定義する要件は満たす）
- `try/except` など指示以上の実装（モジュール不使用なら可）
- コメント・空行の有無
- `print` を使わずセルの戻り値として表示（問題文に print の明示がない場合）
- 数値を文字列で代入（表示が正常なら可）
- `print` のカンマ区切りによる余分な半角スペース

## 迷った場合

減点せず `要判断リスト.txt` へ回す。学籍番号・問題番号・論点・暫定点を書く。
勝手に判断を確定させない。

## 裁定履歴

- 2026-09-18 / 01k: `print` を使わずセルの戻り値として表示
  → **減点なし**。問題文に print の明示がなく、結果は表示されている
- 2026-09-18 / 01k: 数値を文字列で代入（表示は正常）
  → **減点なし**。初期の回であり出力はできている
- 2026-09-18 / 01k: `print` のカンマ区切りで余分な半角スペース
  → **減点なし**
- 2026-09-18 / 方針追加: あからさまに高度なモジュールを使った解答は 0点

---

# 技術メモ

- zip内のipynbは `001_24X5048_#U9234#U6728_..._01k.ipynb` の形式。
  学籍番号はファイル名の2番目のフィールドで取れるので氏名のデコードは不要
- 問題番号の紐付けはmarkdownセルの `k01_N` / `p01_N` を正規表現で拾う。
  コードセルの順番だけで数えるとセル欠落時にズレる（01kで実際に発生）
- 出力は `output_type` で分岐
  - `stream` → stdout
  - `error` → 0点
  - `execute_result` → セルの戻り値（`print` 無しでも出力あり）
- csvは `utf-8-sig` で読み書き。列位置は決め打ちせず、ヘッダ行（`id` で始まる行）から
  `USERID` / `point` / `comment` を引く
- 提出が `.html` や `.py` の学生はipynbが無いので採点不能 → 要判断リストへ
- 大学システムへは「レポート/記述式問題の採点」画面から採点済csvをアップロード。
  `id` と `contents_id` は改変しないこと

# 実績

- 01k（2026-09-18、13人）: 練習。満点9人、減点4人。
  検出できた不備は解答セル欠落2件、print漏れ1件、要件未達1件、定数ミス1件
